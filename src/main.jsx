import React from "react";
import { createRoot } from "react-dom/client";
import EUQuizEntryPage from "./EUQuizEntryPage";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EUQuizEntryPage />
  </React.StrictMode>
);
